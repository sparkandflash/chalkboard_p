# Frontend Rules

1. **No Icons**: Use words instead of icons for all interactive elements and labels.
2. **Words instead of Buttons**: Use clickable words (e.g., plain text links or text-only buttons with hover underlines) instead of traditional button styles (borders, backgrounds). 

Apply these rules consistently across all components.
